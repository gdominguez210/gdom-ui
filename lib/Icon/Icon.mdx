import { Meta, Primary, Controls, Stories, Story, Canvas, Source } from '@storybook/blocks';
import * as IconStories from './Icon.stories';
import { DocumentationNav } from '@storybook-components/DocumentationNav/DocumentationNav';
import { ViewSource } from '@storybook-components/ViewSource/ViewSource';

<Meta of={IconStories} />

# Icon

<ViewSource href="Icon" />

The Icon component is a polymorphic, tree-shakable SVG icon system that provides a consistent way to include icons in your UI.

<DocumentationNav />
<Primary />
<Controls />

## Features

The Icon component offers the following features:

- **Tree-shakable architecture** for optimized bundle size
- **Consistent sizing** relative to text using `em` units
- **Customizable colors** via Tailwind text color utilities
- **Accessibility support** with proper ARIA attributes
- **TypeScript support** with type safety
- **Scalable vector graphics** that maintain quality at any size
- **Support for custom SVGs** with consistent styling

## Examples

<Stories includePrimary={false} title=""/>

## Architecture

The Icon component is implemented as a polymorphic component that accepts an SVG component:

<Source
  language="tsx"
  dark
  code={`
import { Icon } from '@gdominguez210/gdom-ui/Icon';
import { StarLine, PlayLargeFill } from '@gdominguez210/gdom-ui/icons';

// Recommended usage with named icon components (tree-shakable)
<StarLine />
<PlayLargeFill className="text-blue-500" />

// Alternative usage with base Icon component
<Icon as={StarLine} />
<Icon as={PlayLargeFill} className="text-blue-500" />
`}
/>

## Using Custom SVGs

You can use your own SVG components with the Icon component:

<Source
  language="tsx"
  dark
  code={`
import { Icon } from '@gdominguez210/gdom-ui/Icon';
import { ReactComponent as CustomSvg } from './path/to/custom.svg';

<Icon as={CustomSvg} className="text-green-500" />
`}
/>

This allows you to maintain consistent styling across all icons in your application, whether they come from this library or from custom sources.

### Styling Approach

The Icon component:

- Uses the `fill-current` utility to inherit color from text color
- Sets height to `1em` by default, making it scale with text size
- Supports all SVG attributes like `className`, `aria-label`, etc.

<Source
  language="tsx"
  dark
  code={`
// Icons inherit text color
<div className="text-red-500">
  <StarLine />
</div>

// Icons inherit text size
<div className="text-2xl">
  <PlayLargeFill />
</div>

// Custom classes can override defaults
<StarLine className="h-8 w-8 text-blue-500" />
`}
/>

### Bundle Size Optimization

The Icon component is designed for optimal bundle size:

- Each icon is exported as its own component, enabling tree-shaking
- Only the icons you import are included in your bundle
- For applications where bundle size is critical, use the direct imports

<Source
  language="tsx"
  dark
  code={`
// Only these specific icons will be included in your bundle
import { IconPlayLargeFill } from '@gdominguez210/gdom-ui/IconPlayLargeFill';
import { IconPauseLargeFill } from '@gdominguez210/gdom-ui/IconPauseLargeFill';
`}
/>

For internal usage where bundle size is less critical (like documentation or admin panels), consider using the `IconLibrary` component instead, which allows referencing icons by name.