import { Meta, Controls, Story, Source, Canvas, Stories } from '@storybook/blocks';
import * as CanvasResponsiveStories from './CanvasResponsive.stories';
import { DocumentationNav } from '@storybook-components/DocumentationNav/DocumentationNav';

<Meta of={CanvasResponsiveStories} />

# CanvasResponsive

A responsive canvas component that automatically manages canvas resolution and scaling based on container dimensions, device pixel ratio, and window resize events.

<DocumentationNav />

## Features

The CanvasResponsive component provides several key features:

- **High-DPI Support:** Automatically adjusts canvas resolution based on the device pixel ratio, ensuring crisp rendering on retina and high-DPI displays. This prevents the blurry appearance common with canvas elements on high-resolution screens.
  <br />
- **Coordinate System Normalization:** Sets up the canvas context with a transformation matrix that maintains a 1:1 coordinate system, regardless of the device pixel ratio. This means you can use the same coordinates in your drawing code regardless of the display density.
  <br />
- **Responsive Resizing:** Uses ResizeObserver to detect when the canvas's container size changes and automatically adjusts the canvas dimensions accordingly. This ensures the canvas always fills its container properly and maintains the correct aspect ratio.
  <br />
- **Performance Optimization:** Throttles resize operations to prevent performance issues during continuous resize events, with an optional framerate limit.
  <br />

## Stories

<div className="p-3 rounded-md border border-slate-400 bg-slate-100 text-slate-400">

<div className="font-bold text-slate-700">Usage Note</div>
<p>
  CanvasResponsive is a utility component that manages canvas scaling and resolution, but doesn't
  render any content by itself. You'll need to use the Canvas API to draw on it. The examples below
  demonstrate how the component handles different device pixel ratios and container sizes.
</p>

</div>

{/* Render stories with increased height to ensure text is fully visible */}

<Stories title="" />

## Component Architecture

The CanvasResponsive component consists of two main parts:

- **CanvasResponsive Component**: A React component that renders a canvas element with the appropriate props and refs.
- **useCanvasResponsive Hook**: A custom hook that handles the canvas scaling and resizing logic.

This separation allows for flexibility - you can use either the full component or just the hook in your own canvas implementation.

### Using the Component

The simplest way to use the responsive canvas functionality is to use the CanvasResponsive component directly:

<Source
  language="tsx"
  dark
  code={`
import { CanvasResponsive } from '@gdominguez210/gdom-ui/CanvasResponsive';
import { useRef, useEffect } from 'react';

const MyCanvasComponent = () => {
const canvasRef = useRef<HTMLCanvasElement>(null);

useEffect(() => {
const canvas = canvasRef.current;
if (!canvas) return;

    const ctx = canvas.getContext('2d');
    if (!ctx) return;

    // Your drawing code here
    ctx.fillStyle = 'blue';
    ctx.fillRect(10, 10, 100, 100);

}, []);

return <CanvasResponsive ref={canvasRef} className="w-full h-64" />;
};
`} />

### Using the Hook

For more custom implementations, you can use the useCanvasResponsive hook directly with your own canvas element:

<Source
  language="tsx"
  dark
  code={`
import { useCanvasResponsive } from '@gdominguez210/gdom-ui/CanvasResponsive';
import { useEffect } from 'react';

const MyCustomCanvas = () => {
const canvasRef = useCanvasResponsive({ frameRate: 30 });

useEffect(() => {
const canvas = canvasRef.current;
if (!canvas) return;

    const ctx = canvas.getContext('2d');
    if (!ctx) return;

    // Your drawing code here
    ctx.fillStyle = 'red';
    ctx.fillRect(10, 10, 100, 100);

}, []);

return <canvas ref={canvasRef} className="w-full h-64 border border-gray-300" />;
};
`} />

## Technical Details

### Resolution Scaling

The component handles resolution scaling by:

- Reading the device pixel ratio (window.devicePixelRatio)
- Setting the canvas width and height to clientWidth/Height \* devicePixelRatio
- Applying a scaling transformation to the context

This process ensures that:

- The canvas has the correct number of pixels for the display density
- The coordinate system remains normalized (1px in your code = 1px visual size)
- Drawing operations appear crisp on high-DPI displays

### ResizeObserver Integration

The component uses ResizeObserver to detect size changes, which is more efficient than window resize events and works for all container resizing scenarios, not just window resizing.

### Performance Throttling

The component uses a requestAnimationFrame-based throttling mechanism to optimize resize handling:

## Props

<Controls />
