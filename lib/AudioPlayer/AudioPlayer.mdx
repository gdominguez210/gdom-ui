import { Meta, Controls, Canvas as StorybookCanvas, Source, Stories, Primary, ArgTypes } from '@storybook/addon-docs/blocks';
import * as AudioPlayerStories from './AudioPlayer.stories';
import { DocumentationNav } from '@storybook-components/DocumentationNav/DocumentationNav';
import { ViewSource } from '@storybook-components/ViewSource/ViewSource';
import { AudioPlayerCompoundComponent as AudioPlayer } from './namespace'
import { Canvas } from '@storybook-components/Canvas/Canvas';
import { UsageNote } from '@storybook-components/UsageNote/UsageNote';
import { AudioPlayerContextPlaybackProvider } from '@/lib/AudioPlayerContextPlaybackProvider/AudioPlayerContextPlaybackProvider';
import { AudioPlayerContextRefsProvider } from '@/lib/AudioPlayerContextRefsProvider/AudioPlayerContextRefsProvider';
import { AudioPlayerContextTrackProvider } from '@/lib/AudioPlayerContextTrackProvider/AudioPlayerContextTrackProvider';
import { AudioPlayerContextTimeProvider } from '@/lib/AudioPlayerContextTimeProvider/AudioPlayerContextTimeProvider';

<Meta of={AudioPlayerStories} />

# AudioPlayer

<ViewSource href="AudioPlayer" />

The AudioPlayer is a flexible, composable audio playback interface that provides a comprehensive set of controls and display elements for audio content. Built using a compound component pattern, it allows for maximum customization and flexibility in implementation.

<DocumentationNav />
<StorybookCanvas of={AudioPlayerStories.Base} />

## Features

- [**Two-Tier Architecture**](./?path=/docs/core-concepts-two-tier-components--docs): Context-connected components for state management and primitive components for presentation.

- [**Polymorphic Components**](./?path=/docs/core-concepts-polymorphic-components--docs): Render as different HTML elements or React components using the `as` prop.

- **Consistent Event Handling**: Internal handlers run first to maintain core functionality, then your custom handlers are called.

- **Comprehensive Controls**: Complete suite of audio playback controls including play/pause, volume, seek, and track navigation.

- **Flexible Layout**: Arrange components in any layout and style to match your design system.

- **Accessibility-First**: Built with proper ARIA attributes, keyboard navigation, and screen reader support.

See the [Core Concepts](./?path=/docs/core-concepts-introduction--docs) for more details on the architectural patterns used throughout GDOM-UI.

## Examples

<UsageNote>
  The examples below incorporate responsive CSS to ensure optimal display across devices. These responsive styles are **not included by default in the AudioPlayer components**.

  Consumers are responsible for implementing responsive styling within their own applications. This process is straightforward due to the flexible styling methodology outlined on the [Styling Approach](./?path=/docs/core-concepts-styling-approach--docs) page.
</UsageNote>

<Stories
  includePrimary={false}
  title=""
/>

## Available Subcomponents

Each component is available through the main `AudioPlayer` namespace or can be imported directly. Primitive components are also available for advanced customization.

<Canvas>
<>
### Root Container

<ViewSource href="AudioPlayer/AudioPlayer" />

The main wrapper component that provides layout and styling for the audio player.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.Root>...</AudioPlayer.Root>
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer/AudioPlayer';
<AudioPlayer>...</AudioPlayer>
`} />

<Source
  language="tsx"
  dark
  code={`
// Primitive component
import { AudioPlayerPrimitive } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayerPrimitive>...</AudioPlayerPrimitive>
`} />

#### API Reference

`type ComponentPropsWithRef<'T extends ElementType = 'div'>` &
<ArgTypes of={AudioPlayer.Root} />
</>
</Canvas>

<Canvas>
<>
### Provider

<ViewSource href="AudioPlayerContextProvider" />

Provides audio state and context to all child components. Required at the root level.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.Provider tracks={tracks}>...</AudioPlayer.Provider>
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerContextProvider } from '@gdominguez210/gdom-ui/AudioPlayerContextProvider';
<AudioPlayerContextProvider tracks={tracks}>...</AudioPlayerContextProvider>
`} />

<UsageNote>
The `AudioPlayer.Provider` is a composed component that combines multiple context providers to manage the state of the audio player:

- [AudioPlayerContextRefsProvider](#refs-provider) - Manages references to the audio element
- [AudioPlayerContextTrackProvider](#track-provider) - Handles track selection and management
- [AudioPlayerContextTimeProvider](#time-provider) - Manages time-related state (current time, duration)
- [AudioPlayerContextPlaybackProvider](#playback-provider) - Controls playback state (playing, volume, loop, shuffle)

</UsageNote>

#### API Reference

`type PropsWithChildren` &
<ArgTypes of={AudioPlayer.Provider} />

The `tracks` prop required by the Provider accepts an array of objects with the following structure:

**AudioTrackData**
| Property  | Type     | Required | Default | Description                              |
|-----------|----------|----------|---------|------------------------------------------|
| id        | string   | Yes      | -       | Unique identifier for the track          |
| title     | string   | Yes      | -       | Title of the track                       |
| src       | string   | Yes      | -       | URL to the audio file                    |
| author    | string   | Yes      | -       | Name of the artist or author             |
| thumbnail | string   | No       | -       | URL to a thumbnail image for the track   |
| `[key]`     | unknown  | No       | -       | Additional properties of any valid type  |

</>
</Canvas>

<Canvas>
<>
### Refs Provider

<ViewSource href="AudioPlayerContextRefsProvider" />

Manages references to the audio element. 

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerContextRefsProvider } from '@gdominguez210/gdom-ui/AudioPlayerContextRefsProvider';
<AudioPlayerContextRefsProvider>...</AudioPlayerContextRefsProvider>
`} />

<UsageNote>
This provider is not exposed on the `AudioPlayer` namespace and is primarily available for custom implementations that need direct access to track management functionality.
</UsageNote>

#### API Reference

`type PropsWithChildren`

#### Related Hooks

- `useAudioPlayerContextRefs` - Hook to access audio element references and progress bar reference

| Return Value | Type | Description |
|--------------|------|-------------|
| audioRef | `RefObject<HTMLAudioElement \| null>` | Reference to the audio element |
| progressBarRef | `RefObject<HTMLInputElement \| null>` | Reference to the progress bar input element |
</>
</Canvas>

<Canvas>
<>
### Track Provider

<ViewSource href="AudioPlayerContextTrackProvider" />

Handles track selection and management. 

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerContextTrackProvider } from '@gdominguez210/gdom-ui/AudioPlayerContextTrackProvider';
<AudioPlayerContextTrackProvider tracks={tracks}>...</AudioPlayerContextTrackProvider>
`} />

<UsageNote>
This provider is not exposed on the `AudioPlayer` namespace and is primarily available for custom implementations that need direct access to track management functionality.
</UsageNote>

#### API Reference

`type PropsWithChildren` &
<ArgTypes of={AudioPlayerContextTrackProvider} />

#### AudioTrackData

| Property   | Type     | Required | Default | Description                                 |
|------------|----------|----------|---------|---------------------------------------------|
| id         | string   | Yes      | -       | Unique identifier for the track             |
| title      | string   | Yes      | -       | Title of the track                          |
| src        | string   | Yes      | -       | Source URL for the audio file               |
| author     | string   | Yes      | -       | Name of the track's author                  |
| thumbnail  | string   | Yes      | -       | URL for the track's thumbnail image         |
| `[key]`        | unknown  | No       | -       | Additional properties of any type are valid |

#### Related Hooks

- `useAudioPlayerContextTrack` - Hook to access track-related state and functions (current track, track index, track list)

| Return Value | Type | Description |
|--------------|------|-------------|
| currentTrack | `AudioTrackData \| undefined` | The currently playing track |
| currentTrackIndex | `number` | Index of the current track in the playlist |
| tracks | `AudioTrackData[]` | Array of all available tracks |
| setTrackIndex | `(index: number) => void` | Function to change the current track |
</>
</Canvas>

<Canvas>
<>
### Time Provider

<ViewSource href="AudioPlayerContextTimeProvider" />

Manages time-related state (current time, duration). 

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerContextTimeProvider } from '@gdominguez210/gdom-ui/AudioPlayerContextTimeProvider';
<AudioPlayerContextTimeProvider>...</AudioPlayerContextTimeProvider>
`} />

<UsageNote>
This provider is not exposed on the `AudioPlayer` namespace and is primarily available for custom implementations that need direct access to track management functionality.
</UsageNote>

#### API Reference

`type PropsWithChildren` &
<ArgTypes of={AudioPlayerContextTimeProvider} />

#### Related Hooks

- `useAudioPlayerContextTime` - Hook to access time-related state and functions (current time, duration, seek, preview time)

| Return Value | Type | Description |
|--------------|------|-------------|
| currentTime | `number` | Current playback time in seconds |
| duration | `number` | Total duration of the track in seconds |
| previewTime | `number \| null` | Time value when hovering over progress bar |
| seek | `(time: number) => void` | Function to seek to a specific time |
| setDuration | `(duration: number) => void` | Function to update the total duration |
| setPreviewTime | `(time: number \| null) => void` | Function to update preview time |
</>
</Canvas>

<Canvas>
<>
### Playback Provider

<ViewSource href="AudioPlayerContextPlaybackProvider" />

Controls playback state (playing, volume, loop, shuffle). 

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerContextPlaybackProvider } from '@gdominguez210/gdom-ui/AudioPlayerContextPlaybackProvider';
<AudioPlayerContextPlaybackProvider>...</AudioPlayerContextPlaybackProvider>
`} />

<UsageNote>
This provider is not exposed on the `AudioPlayer` namespace and is primarily available for custom implementations that need direct access to track management functionality.
</UsageNote>

#### API Reference

`type PropsWithChildren` &
<ArgTypes of={AudioPlayerContextPlaybackProvider} />

#### Related Hooks

- `useAudioPlayerContextPlayback` - Hook to access playback-related state and functions (playing, volume, loop, shuffle)

| Return Value | Type | Description |
|--------------|------|-------------|
| isPlaying | `boolean` | Whether the audio is currently playing |
| volume | `number` | Current volume level (0-1) |
| mute | `boolean` | Whether the audio is muted |
| shuffle | `boolean` | Whether shuffle mode is enabled |
| loop | `boolean` | Whether loop mode is enabled |
| play | `() => void` | Function to start playback |
| pause | `() => void` | Function to pause playback |
| togglePlay | `() => void` | Function to toggle play/pause |
| setVolume | `(volume: number) => void` | Function to set volume level |
| setMute | `(mute: boolean) => void` | Function to set mute state |
| toggleMute | `() => void` | Function to toggle mute |
| setShuffle | `(shuffle: boolean) => void` | Function to set shuffle state |
| toggleShuffle | `() => void` | Function to toggle shuffle |
| setLoop | `(loop: boolean) => void` | Function to set loop state |
| toggleLoop | `() => void` | Function to toggle loop |
</>
</Canvas>

<Canvas>
<>
### Info Container

<ViewSource href="AudioPlayerInfo" />

Container for track information such as image, title, and author.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.Info>...</AudioPlayer.Info>
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerInfo } from '@gdominguez210/gdom-ui/AudioPlayerInfo';
<AudioPlayerInfo>...</AudioPlayerInfo>
`} />

#### API Reference

`type ComponentPropsWithRef<T extends ElementType = 'div'>` &
<ArgTypes of={AudioPlayer.Info} />
</>
</Canvas>

<Canvas>
<>
### Image

<ViewSource href="AudioPlayerImage" />

Displays the album artwork or track thumbnail image.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.Image />
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerImage } from '@gdominguez210/gdom-ui/AudioPlayerImage';
<AudioPlayerImage />
`} />

<Source
  language="tsx"
  dark
  code={`
// Primitive component
import { AudioPlayerImagePrimitive } from '@gdominguez210/gdom-ui/AudioPlayerImage';
<AudioPlayerImagePrimitive
  src="path/to/image.jpg"
  alt="Album cover"
/>
`} />

#### API Reference

`type ComponentPropsWithRef<'img'>` &
<ArgTypes of={AudioPlayer.Image} />
</>
</Canvas>

<Canvas>
<>
### Title

<ViewSource href="AudioPlayerTitle" />

Displays the track title from the current track.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.Title />
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerTitle } from '@gdominguez210/gdom-ui/AudioPlayerTitle';
<AudioPlayerTitle />
`} />

<Source
  language="tsx"
  dark
  code={`
// Primitive component
import { AudioPlayerTitlePrimitive } from '@gdominguez210/gdom-ui/AudioPlayerTitle';
<AudioPlayerTitlePrimitive>Song Title</AudioPlayerTitlePrimitive>
`} />

#### API Reference

`type ComponentPropsWithRef<T extends ElementType = 'div'>` &
<ArgTypes of={AudioPlayer.Title} />
</>
</Canvas>

<Canvas>
<>
### Author

<ViewSource href="AudioPlayerAuthor" />

Displays the track artist/author name from the current track.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.Author />
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerAuthor } from '@gdominguez210/gdom-ui/AudioPlayerAuthor';
<AudioPlayerAuthor />
`} />

<Source
  language="tsx"
  dark
  code={`
// Primitive component
import { AudioPlayerAuthorPrimitive } from '@gdominguez210/gdom-ui/AudioPlayerAuthor';
<AudioPlayerAuthorPrimitive>Artist Name</AudioPlayerAuthorPrimitive>
`} />

#### API Reference

`type ComponentPropsWithRef<T extends ElementType = 'div'>` &
<ArgTypes of={AudioPlayer.Author} />
</>
</Canvas>

<Canvas>
<>
### Time

<ViewSource href="AudioPlayerTime" />

Displays current playback time and total duration in MM:SS format.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.Time />
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerTime } from '@gdominguez210/gdom-ui/AudioPlayerTime';
<AudioPlayerTime />
`} />

<Source
  language="tsx"
  dark
  code={`
// Primitive component
import { AudioPlayerTimePrimitive } from '@gdominguez210/gdom-ui/AudioPlayerTime';
<AudioPlayerTimePrimitive
  currentTime={60}
  duration={180}
/>
`} />

#### API Reference

`type ComponentPropsWithRef<T extends ElementType = 'span'>` &
<ArgTypes of={AudioPlayer.Time} />

#### Related Hooks

- `useAudioPlayerTime` - Hook to format and manage time display (current time and duration)

| Return Value | Type | Description |
|--------------|------|-------------|
| currentTimeDisplay | `string` | Current time formatted as MM:SS |
| durationDisplay | `string` | Duration formatted as MM:SS |
</>
</Canvas>

<Canvas>
<>
### Controls Container

<ViewSource href="AudioPlayerControls" />

Flexible container for grouping playback control buttons.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.Controls>...</AudioPlayer.Controls>
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerControls } from '@gdominguez210/gdom-ui/AudioPlayerControls';
<AudioPlayerControls>...</AudioPlayerControls>
`} />

#### API Reference

`type ComponentPropsWithRef<T extends ElementType = 'div'>` &
<ArgTypes of={AudioPlayer.Controls} />
</>
</Canvas>

<Canvas>
<>
### Control Play

<ViewSource href="AudioPlayerControlPlay" />

Toggles playback state with dynamic icon that changes between play and pause.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.ControlPlay />
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerControlPlay } from '@gdominguez210/gdom-ui/AudioPlayerControlPlay';
<AudioPlayerControlPlay />
`} />

<Source
  language="tsx"
  dark
  code={`
// Primitive component
import { AudioPlayerControlPlayPrimitive } from '@gdominguez210/gdom-ui/AudioPlayerControlPlay';
<AudioPlayerControlPlayPrimitive
  isPlaying={true}
  onClick={() => {}}
/>
`} />

#### API Reference

`type ComponentPropsWithRef<'button'>`

#### Related Hooks

- `useAudioPlayerControlPlay` - Hook to manage play/pause state and handle click events

| Return Value | Type | Description |
|--------------|------|-------------|
| isPlaying | `boolean` | Whether the audio is currently playing |
| handleClick | `(event: React.MouseEvent) => void` | Click handler for play/pause toggle |
</>
</Canvas>

<Canvas>
<>
### Control Next

<ViewSource href="AudioPlayerControlNext" />

Button to skip to the next track in the playlist.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.ControlNext />
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerControlNext } from '@gdominguez210/gdom-ui/AudioPlayerControlNext';
<AudioPlayerControlNext />
`} />

<Source
  language="tsx"
  dark
  code={`
// Primitive component
import { AudioPlayerControlNextPrimitive } from '@gdominguez210/gdom-ui/AudioPlayerControlNext';
<AudioPlayerControlNextPrimitive onClick={() => {}} />
`} />

#### API Reference

`type ComponentPropsWithRef<'button'>`

#### Related Hooks

- `useAudioPlayerNextTrack` - Hook to handle next track navigation and click events

| Return Value | Type | Description |
|--------------|------|-------------|
| handleClick | `(event: React.MouseEvent) => void` | Click handler for next track navigation |
| isLastTrack | `boolean` | Whether the current track is the last in the playlist |
</>
</Canvas>

<Canvas>
<>
### Control Previous

<ViewSource href="AudioPlayerControlPrevious" />

Button to return to the previous track or restart the current track.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.ControlPrevious />
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerControlPrevious } from '@gdominguez210/gdom-ui/AudioPlayerControlPrevious';
<AudioPlayerControlPrevious />
`} />

<Source
  language="tsx"
  dark
  code={`
// Primitive component
import { AudioPlayerControlPreviousPrimitive } from '@gdominguez210/gdom-ui/AudioPlayerControlPrevious';
<AudioPlayerControlPreviousPrimitive onClick={() => {}} />
`} />

#### API Reference

`type ComponentPropsWithRef<'button'>`

#### Related Hooks

- `useAudioPlayerPreviousTrack` - Hook to handle previous track navigation and click events

| Return Value | Type | Description |
|--------------|------|-------------|
| handleClick | `(event: React.MouseEvent) => void` | Click handler for previous track navigation |
| isFirstTrack | `boolean` | Whether the current track is the first in the playlist |
</>
</Canvas>

<Canvas>
<>
### Control Loop

<ViewSource href="AudioPlayerControlLoop" />

Toggles repeat mode for single track or playlist with visual indicator.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.ControlLoop />
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerControlLoop } from '@gdominguez210/gdom-ui/AudioPlayerControlLoop';
<AudioPlayerControlLoop />
`} />

<Source
  language="tsx"
  dark
  code={`
// Primitive component
import { AudioPlayerControlLoopPrimitive } from '@gdominguez210/gdom-ui/AudioPlayerControlLoop';
<AudioPlayerControlLoopPrimitive
  active={true}
  onClick={() => {}}
/>
`} />

#### API Reference

`type ComponentPropsWithRef<'button'>`

</>
</Canvas>

<Canvas>
<>
### Control Shuffle

<ViewSource href="AudioPlayerControlShuffle" />

Toggles shuffle playback mode with visual indicator for active state.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.ControlShuffle />
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerControlShuffle } from '@gdominguez210/gdom-ui/AudioPlayerControlShuffle';
<AudioPlayerControlShuffle />
`} />

<Source
  language="tsx"
  dark
  code={`
// Primitive component
import { AudioPlayerControlShufflePrimitive } from '@gdominguez210/gdom-ui/AudioPlayerControlShuffle';
<AudioPlayerControlShufflePrimitive
  active={true}
  onClick={() => {}}
/>
`} />

#### API Reference

`type ComponentPropsWithRef<'button'>`

</>
</Canvas>

<Canvas>
<>
### Control Audio

<ViewSource href="AudioPlayerControlAudio" />

The core HTML audio element that handles actual audio playback (typically hidden).

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.ControlAudio />
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerControlAudio } from '@gdominguez210/gdom-ui/AudioPlayerControlAudio';
<AudioPlayerControlAudio />
`} />

<Source
  language="tsx"
  dark
  code={`
// Primitive component
import { AudioPlayerControlAudioPrimitive } from '@gdominguez210/gdom-ui/AudioPlayerControlAudio';
<AudioPlayerControlAudioPrimitive src="path/to/audio.mp3" />
`} />

#### API Reference

`type ComponentPropsWithRef<'audio'>`

#### Related Hooks

- `useAudioPlayerMetadata` - Hook to access and manage audio metadata

| Return Value | Type | Description |
|--------------|------|-------------|
| metadata | `AudioMetadata` | Object containing audio metadata (duration, title, etc.) |
| updateMetadata | `(metadata: Partial<AudioMetadata>) => void` | Function to update metadata |
</>
</Canvas>

<Canvas>
<>
### Progress Bar

<ViewSource href="AudioPlayerProgressBar" />

Interactive slider that displays and controls playback progress.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.ProgressBar />
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerProgressBar } from '@gdominguez210/gdom-ui/AudioPlayerProgressBar';
<AudioPlayerProgressBar />
`} />

<Source
  language="tsx"
  dark
  code={`
// Primitive component
import { AudioPlayerProgressBarPrimitive } from '@gdominguez210/gdom-ui/AudioPlayerProgressBar';
<AudioPlayerProgressBarPrimitive
  value={50}
  max={100}
  onChange={() => {}}
/>
`} />

#### API Reference

`type ComponentPropsWithRef<'input'>`

#### Related Hooks

- `useAudioPlayerProgressBar` - Hook to manage progress bar state and interactions

| Return Value | Type | Description |
|--------------|------|-------------|
| handleProgressChange | `(event: React.ChangeEvent<HTMLInputElement>) => void` | Handler for progress bar value changes |
| handleMouseEnter | `(event: React.MouseEvent<HTMLInputElement>) => void` | Handler for mouse enter events |
| handleMouseMove | `(event: React.MouseEvent<HTMLInputElement>) => void` | Handler for mouse move events |
| handleMouseOut | `(event: React.MouseEvent<HTMLInputElement>) => void` | Handler for mouse out events |
| elementRef | `RefObject<HTMLInputElement>` | Reference to the progress bar input element |
</>
</Canvas>

<Canvas>
<>
### Progress Waveform

<ViewSource href="AudioPlayerProgressWaveform" />

An enhanced progress control that displays a waveform visualization of the audio and allows seeking by clicking on different parts of the waveform.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.ProgressWaveform waveformData={waveformData} />
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerProgressWaveform } from '@gdominguez210/gdom-ui/AudioPlayerProgressWaveform';
<AudioPlayerProgressWaveform waveformData={waveformData} />
`} />

<Source
  language="tsx"
  dark
  code={`
// With custom styling
<AudioPlayerProgressWaveform 
  waveformData={waveformData}
  progressColor="rgba(59, 130, 246, 1)"
  backgroundColor="rgba(229, 231, 235, 0.5)"
  className="h-12 w-full rounded-md"
/>
`} />

#### API Reference

[AudioProgressWaveform](/docs/AudioProgressWaveform--docs)

</>
</Canvas>

<Canvas>
<>
### Volume Container

<ViewSource href="AudioPlayerVolume" />

Container for volume controls that can be configured to expand/collapse.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.Volume>...</AudioPlayer.Volume>
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerVolume } from '@gdominguez210/gdom-ui/AudioPlayerVolume';
<AudioPlayerVolume>...</AudioPlayerVolume>
`} />

<Source
  language="tsx"
  dark
  code={`
// Primitive component
import { AudioPlayerVolumePrimitive } from '@gdominguez210/gdom-ui/AudioPlayerVolume';
<AudioPlayerVolumePrimitive>...</AudioPlayerVolumePrimitive>
`} />

#### API Reference

<ArgTypes of={AudioPlayer.Volume} />
</>
</Canvas>

<Canvas>
<>
### Volume Button

<ViewSource href="AudioPlayerVolumeButton" />

Button that toggles mute state with dynamic icon based on volume level.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.VolumeButton />
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerVolumeButton } from '@gdominguez210/gdom-ui/AudioPlayerVolumeButton';
<AudioPlayerVolumeButton />
`} />

<Source
  language="tsx"
  dark
  code={`
// Primitive component
import { AudioPlayerVolumeButtonPrimitive } from '@gdominguez210/gdom-ui/AudioPlayerVolumeButton';
<AudioPlayerVolumeButtonPrimitive
  muted={false}
  onClick={() => {}}
/>
`} />

#### API Reference

`type ComponentPropsWithRef<'button'>`

</>
</Canvas>

<Canvas>
<>
### Volume Slider

<ViewSource href="AudioPlayerVolumeSlider" />

Interactive slider that displays and controls audio volume level.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.VolumeSlider />
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerVolumeSlider } from '@gdominguez210/gdom-ui/AudioPlayerVolumeSlider';
<AudioPlayerVolumeSlider />
`} />

<Source
  language="tsx"
  dark
  code={`
// Primitive component
import { AudioPlayerVolumeSliderPrimitive } from '@gdominguez210/gdom-ui/AudioPlayerVolumeSlider';
<AudioPlayerVolumeSliderPrimitive
  value={75}
  onChange={() => {}}
/>
`} />

#### API Reference

`type ComponentPropsWithRef<'input'>` &
<ArgTypes of={AudioPlayer.VolumeSlider} />
</>
</Canvas>

<Canvas>
<>
### Visualizer Waveform

<ViewSource href="AudioPlayerVisualizerWaveform" />

Displays a real-time waveform visualization of the audio being played.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.VisualizerWaveform />
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerVisualizerWaveform } from '@gdominguez210/gdom-ui/AudioPlayerVisualizerWaveform';
<AudioPlayerVisualizerWaveform />
`} />

<Source
  language="tsx"
  dark
  code={`
// With custom styling
<AudioPlayerVisualizerWaveform 
  lineColor="rgba(59, 130, 246, 1)"
  lineWidth={2}
  className="h-24 w-full rounded-md bg-gray-100"
/>
`} />

#### API Reference

[AudioVisualizerWaveform](/docs/components-AudioVisualizerWaveform--docs)

</>
</Canvas>

<Canvas>
<>
### Visualizer Frequency Bars

<ViewSource href="AudioPlayerVisualizerFrequencyBars" />

Displays a real-time frequency spectrum visualization as animated vertical bars. Shows different frequency ranges of the audio being played, similar to an equalizer display.

#### Usage

<Source
  language="tsx"
  dark
  code={`
// Compound component usage
import { AudioPlayer } from '@gdominguez210/gdom-ui/AudioPlayer';
<AudioPlayer.VisualizerFrequencyBars />
`} />

<Source
  language="tsx"
  dark
  code={`
// Direct import
import { AudioPlayerVisualizerFrequencyBars } from '@gdominguez210/gdom-ui/AudioPlayerVisualizerFrequencyBars';
<AudioPlayerVisualizerFrequencyBars />
`} />

<Source
  language="tsx"
  dark
  code={`
// With custom styling and options
<AudioPlayerVisualizerFrequencyBars 
  barColor="rgba(59, 130, 246, 1)"
  barCount={32}
  barGapRatio={0.3}
  heightMultiplier={1.5}
  minBarHeight={3}
  colorMode="gradient"
  className="h-24 w-full rounded-md bg-gray-100"
/>
`} />

<Source
  language="tsx"
  dark
  code={`
// With rainbow color effect
<AudioPlayerVisualizerFrequencyBars 
  colorMode="rainbow"
  barCount={64}
  className="h-36 w-full rounded-md"
/>
`} />

#### API Reference

[AudioVisualizerFrequencyBars](/docs/components-AudioVisualizerFrequencyBars--docs)

</>
</Canvas>
